#!/bin/bash

JOBID=`job vcat-toollabs-daemon`

if [ -z "$JOBID" ]; then
	jstart -quiet -stderr -mem 2g -o $HOME/vcat-toollabs-daemon.log -j y -N vcat-toollabs-daemon java -Xms128m -Xmx128m -jar $HOME/work/jar/vcat-toollabs.jar $HOME/work/vcat.properties
fi
