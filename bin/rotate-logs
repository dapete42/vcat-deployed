#!/bin/sh

for log in error.log gridserver.log
do
	if [ -e $HOME/$log ]; then
		cp -p $HOME/$log $HOME/$log.old
		cat /dev/null > $HOME/$log
	fi
done

find $HOME/public_tomcat/logs/ ! -name '.gitignore' -type f -mtime +6 -delete
