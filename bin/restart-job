#!/bin/bash

# Stop the job
$HOME/bin/stop-job

# Wait until it is really gone
JOB=`job vcat-toollabs-daemon`
until [ -z "$JOB" ]; do
	sleep 0.5
	JOB=`job vcat-toollabs-daemon`
done

# Start the job again
$HOME/bin/start-job
